How to use

```bash
git clone https://github.com/webkouza69/php_lamp.git
cd docker-lamp/
docker-compose up -d
```
サービスの構築または再構築
$ docker-compose build -d

コンテナの作成と開始
$ docker-compose up -d

サービスの停止
$ docker-compose stop

コンテナの停止
$ docker-compose down

